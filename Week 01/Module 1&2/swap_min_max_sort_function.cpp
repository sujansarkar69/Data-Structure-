#include <bits/stdc++.h>
using namespace std;
int main()
{
    // Swap function:
    //  int num1, num2;
    //  cin >> num1 >> num2;
    //  swap(num1, num2);
    //  cout << num1 << " " << num2 << endl;

    // Min Function:
    // int a = 3, b = 5, c = 2, d = 9;
    // int minn = min({a, b, c, d});
    // cout << minn << endl;

    // Max function:
    // int a = 3, b = 5, c = 2, d = 9, e = 4, f = 12;
    // int maxx = max({a, b, c, d, e, f});
    // cout << maxx << endl;

    // Sort function:

    // vector<int> a = {1, 4, 5, 2, 6, 3};
    // cout << "Before sort:\n";
    // for (int i = 0; i < a.size(); i++)
    // {
    //     cout << a[i] << " ";
    // }
    // cout << "\nAfter Sort:\n";
    // sort(a.begin(), a.end());
    // for (int i = 0; i < a.size(); i++)
    // {
    //     cout << a[i] << " ";
    // }

    // sorting by index to index:
    // cout << "\nAfter 2nd index to 4th sort:\n";
    // sort(a.begin() + 2, a.begin() + 4 + 1);
    // for (int i = 0; i < a.size(); i++)
    // {
    //     cout << a[i] << " ";
    // }
}