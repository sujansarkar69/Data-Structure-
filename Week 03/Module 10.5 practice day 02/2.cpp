#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int arr[n];
    int freq[10];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
        freq[i]++;
    }

    for (int i = 0; i < freq[i]; i++)
    {
        cout << i << " ";
    }
}